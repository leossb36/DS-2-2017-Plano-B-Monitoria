<%= bootstrap_devise_error_messages! %>
<div class="panel panel-default devise-bs">
  <div class="container-fluid">
    <div class="row mt-10">
      <div class="mx-auto my-auto text-center">
        <div class="card bg-light border-secondary mb-4 z-depth-2 slide-in-blurred-top">
          <div class="card-header">
            <h1 style="font-size:150%;">Cadastro</h1>
          </div>
          <div class="x_panel slide-in-blurred-top" id="backStyle" style="background-color: white">
            <div class="panel-body">
              <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { role: 'form' }) do |f| %>
              <div class="form-group">
                <%= f.label :email %>
                <%= f.email_field :email, autofocus: true, class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.label :senha %>
                <%= f.password_field :password, class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.label :Confirmação_de_senha %>
                <%= f.password_field :password_confirmation, class: 'form-control' %>
              </div>
              <%= f.submit t('.cadastrar', default: 'Cadastrar'), class: 'btn btn-primary' %>
              <% end %>
              <br />
              <br />
              <%= render 'devise/shared/links' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
